<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Делаем фон прозрачным -->
    <stroke
        android:width="2dp"
        android:color="@color/colorPrimary" />
</shape>